import styled from "styled-components";

const FooterHeadingTitle = styled.div`
	display: flex;
	align-items: flex-end;
	justify-content: flex-end;
	flex: 1;
	font-weight: 400;
`;

export default FooterHeadingTitle;
